---
description: Create a validated versioned assignment for transfer to another agent.
---

<!-- code-agent-template:managed -->
# Create Agent Task

Invoke `agent-task` in Author mode. Gather the objective, capabilities, inputs, scope, approval gates, acceptance criteria, and verification. Publish a new `.agents/tasks/<task-name>-v<version>.md`, validate it, and do not execute it.
