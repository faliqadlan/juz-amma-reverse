---
description: Inspect the repository and populate verified project context.
---

<!-- code-agent-template:managed -->
# Onboard Repository

Invoke `onboard-repository`. Inspect evidence before updating `.agents/context/project.md`, separate verified facts from proposals and unknowns, and never record secret values.
